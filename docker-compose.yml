services:
  op-manga-dl-ger:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./downloads:/downloads
    environment:
      DOWNLOAD_ROOT: /downloads
      HEADLESS: "true"
      PAGE_SLEEP: "1.0"
      SCHEDULER_STATE_FILE: /downloads/scheduler_state.json
